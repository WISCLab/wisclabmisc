% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-files.R
\name{dir_sync_down}
\alias{dir_sync_down}
\title{Sync one directory down into another}
\usage{
dir_sync_down(
  path,
  path_new,
  .dry_run = TRUE,
  .delete = FALSE,
  .compare = c("metadata", "md5", "xxhash")
)
}
\arguments{
\item{path}{Source directory.}

\item{path_new}{Destination directory.}

\item{.dry_run}{Whether to preview the sync plan without changing files.
Defaults to \code{TRUE}.}

\item{.delete}{Whether to delete files in \code{path_new} that are absent from
\code{path}. Defaults to \code{FALSE}, so extra destination files are ignored unless
deletion is explicitly enabled.}

\item{.compare}{Method used to determine whether files differ. \code{"metadata"}
compares file size and modification time. \code{"md5"} compares file contents
using \code{\link[tools:md5sum]{tools::md5sum()}}. \code{"xxhash"} compares file contents using
\code{xxhashlite::xxhash_file()}, if the \code{xxhashlite} package is installed.}
}
\value{
A data frame describing the directory sync plan, invisibly. The plan
includes the relative file path, the planned \code{action}, the \code{reason} for the
action, source and destination file metadata, and the source and destination
paths used for copying.
}
\description{
\code{dir_sync_down()} syncs the files in one directory into another directory.
Files that are present in \code{path} but missing from \code{path_new} are copied.
Files that are present in both directories but differ are overwritten.
Files that are present in \code{path_new} but absent from \code{path} are ignored
unless \code{.delete = TRUE}.
}
\details{
By default, this function performs a dry run: it prints a summary of the
sync plan and returns the plan invisibly without changing any files. Set
\code{.dry_run = FALSE} to copy, overwrite, or delete files.

The possible values of \code{action} are:
\itemize{
\item \code{"copy"}: copy a file from \code{path} because it is missing from \code{path_new};
\item \code{"overwrite"}: overwrite a destination file because the source and
destination files differ;
\item \code{"delete"}: delete an extra destination file, when \code{.delete = TRUE};
\item \code{"ignore"}: leave an extra destination file unchanged, when
\code{.delete = FALSE};
\item \code{"none"}: leave a file unchanged.
}

With \code{.compare = "metadata"}, files are treated as different when their
size or modification time differs. With \code{.compare = "md5"} or
\code{.compare = "xxhash"}, files that exist on both sides are compared by
content hash instead. Under hash comparison, files with the same contents
are treated as unchanged even if their modification times differ.
}
\examples{
dir_from <- tempfile()
dir_to <- tempfile()

fs::dir_create(dir_from)
fs::dir_create(dir_to)

writeLines("same", fs::path(dir_from, "same.txt"))
writeLines("same", fs::path(dir_to, "same.txt"))

writeLines("copy me", fs::path(dir_from, "new.txt"))

writeLines("new contents", fs::path(dir_from, "changed.txt"))
writeLines("old contents", fs::path(dir_to, "changed.txt"))

writeLines("extra", fs::path(dir_to, "extra.txt"))

# Preview the sync plan without changing files
plan <- dir_sync_down(dir_from, dir_to)

# Actually copy and overwrite files, but keep extra destination files
dir_sync_down(dir_from, dir_to, .dry_run = FALSE)

# Also delete extra destination files
dir_sync_down(dir_from, dir_to, .dry_run = FALSE, .delete = TRUE)

}
